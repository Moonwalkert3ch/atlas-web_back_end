const { expect } = require('chai');

describe('Chai installation test', () => {
    it('should verify chai is installed and working', () => {
        expect(true).to.be.true;
    });
});

console.log('Chai is installed and working correctly.');
